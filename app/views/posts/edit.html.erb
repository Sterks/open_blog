<%= form_for @post do |post|  %>

  <%= post.label :name %>:<br>
  <%= post.text_field :name %><br>

  <%= post.label :body %>:<br>
  <%= post.text_field :body %><br>

  <%= post.submit 'Сохранить' %>

<% end %>

<%= render 'form' %>
